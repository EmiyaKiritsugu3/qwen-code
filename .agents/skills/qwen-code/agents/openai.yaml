interface:
  display_name: "Qwen Code"
  short_description: "Repo-specific patterns and workflows for qwen-code"
  default_prompt: "Use the qwen-code repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true